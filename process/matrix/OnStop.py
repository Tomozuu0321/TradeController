from general.utility.logger import MatrixFunction,log

@MatrixFunction
def OnStop(Params,sts,evt):
    #import tornado.ioloop
    #tornado.ioloop.IOLoop.current().stop()
    pass
    #多分クリーンアップコードが必要になってくる
